const bookArray = ["book1", "book2", "book3", "book4", "book5", "book6"];
function paginate(items, page) {
  const pages = [];

  const itemsPerPage = 2;

  for (let i = 0; i < bookArray; i++) {}

  //an array of Items to split
  //How many pages
  //Return an array split by pages
}

// function paginate2(books, pages) {
//   const booksPerPage = books / pages;
//   const paginatedArray = [];
//   for (const book of bookArray) {
//     let tempArray = [];
//     tempArray.push(book);
//     paginatedArray.push(tempArray);
//   }
//   return paginatedArray;
//   console.log(paginatedArray);
// }
